# Changelog

## 0.1.0

- Add bounded Linux-first ELF inspection.
- Extract compiler claims from ELF `.comment` records.
- Extract `cargo-auditable` `.dep-v0` dependency provenance with bounded decompression.
- Add explicit claim states, path redaction, discrepancy reporting, text output, and stable JSON output.
- Add fixture, malformed input, stripping, size-bound, and CLI tests.
